# name: Release

# on:
#   push:
#     tags:
#       - 'v*'

# jobs:
#   test:
#     name: Test Before Release
#     runs-on: ubuntu-latest
    
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v4
      
#     - name: Set up Python
#       uses: actions/setup-python@v5
#       with:
#         python-version: "3.11"
        
#     - name: Install uv
#       uses: astral-sh/setup-uv@v4
#       with:
#         enable-cache: true
#         cache-dependency-glob: "uv.lock"
        
#     - name: Install dependencies
#       run: |
#         uv sync --all-extras --dev
        
#     - name: Run tests
#       run: |
#         uv run pytest tests/ --cov=src

#   build:
#     name: Build Package
#     runs-on: ubuntu-latest
#     needs: test
    
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v4
      
#     - name: Set up Python
#       uses: actions/setup-python@v5
#       with:
#         python-version: "3.11"
        
#     - name: Install uv
#       uses: astral-sh/setup-uv@v4
#       with:
#         enable-cache: true
#         cache-dependency-glob: "uv.lock"
        
#     - name: Build package
#       run: |
#         uv build
        
#     - name: Upload build artifacts
#       uses: actions/upload-artifact@v4
#       with:
#         name: python-package-${{ github.ref_name }}
#         path: dist/

#   release:
#     name: Create GitHub Release
#     runs-on: ubuntu-latest
#     needs: build
#     permissions:
#       contents: write
    
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v4
      
#     - name: Download build artifacts
#       uses: actions/download-artifact@v4
#       with:
#         name: python-package-${{ github.ref_name }}
#         path: dist/
        
#     - name: Create Release
#       uses: softprops/action-gh-release@v2
#       with:
#         files: dist/*
#         generate_release_notes: true
#         draft: false
#         prerelease: ${{ contains(github.ref_name, 'rc') || contains(github.ref_name, 'beta') || contains(github.ref_name, 'alpha') }}

#   publish:
#     name: Publish to PyPI
#     runs-on: ubuntu-latest
#     needs: build
#     if: startsWith(github.ref, 'refs/tags/v') && !contains(github.ref_name, 'rc') && !contains(github.ref_name, 'beta') && !contains(github.ref_name, 'alpha')
#     permissions:
#       id-token: write  # For trusted publishing
    
#     steps:
#     - name: Download build artifacts
#       uses: actions/download-artifact@v4
#       with:
#         name: python-package-${{ github.ref_name }}
#         path: dist/
        
#     - name: Publish to PyPI
#       uses: pypa/gh-action-pypi-publish@release/v1
#       with:
#         # You can use either trusted publishing (recommended) or API token
#         # For trusted publishing, configure it in your PyPI account settings
#         password: ${{ secrets.PYPI_API_TOKEN }}  # Remove this line if using trusted publishing 